/** Generated by YAKINDU Statechart Tools code generator. */
package org.yakindu.sct.simulation.core.sexec.test;
import org.eclipse.xtext.junit4.InjectWith;
import org.eclipse.xtext.junit4.XtextRunner;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.yakindu.sct.model.sexec.ExecutionFlow;
import org.yakindu.sct.model.sexec.interpreter.test.util.AbstractExecutionFlowTest;
import org.yakindu.sct.model.sexec.interpreter.test.util.SExecInjectionProvider;
import org.yakindu.sct.test.models.SCTUnitTestModels;
import com.google.inject.Inject;
import static org.junit.Assert.*;

/**
 * Unit TestCase for OutEventVisibilityEventDriven
 */
@SuppressWarnings("all")
@RunWith(XtextRunner.class)
@InjectWith(SExecInjectionProvider.class)
public class OutEventVisibilityEventDriven extends AbstractExecutionFlowTest {
	
	@Before
	public void setup() throws Exception{
		ExecutionFlow flow = models.loadExecutionFlowFromResource("events/OutEventVisibilityEventDriven.sct");
		initInterpreter(flow, true);
	}
	@Test
	public void out_events_must_be_collected_during_local_event_processing_steps() throws Exception {
		interpreter.enter();
		raiseEvent("i", 1l);
		assertTrue(isRaised("o1"));
		assertTrue(isRaised("o2"));
		interpreter.exit();
	}
	@Test
	public void all_out_events_that_are_raised_within_a_single_step_must_be_visible() throws Exception {
		interpreter.enter();
		assertTrue(!isRaised("o1"));
		assertTrue(!isRaised("o2"));
		raiseEvent("i", 2l);
		assertTrue(isRaised("o1"));
		assertTrue(!isRaised("o2"));
		raiseEvent("i", 2l);
		assertTrue(!isRaised("o1"));
		assertTrue(isRaised("o2"));
		raiseEvent("i", 2l);
		assertTrue(isRaised("o1"));
		assertTrue(isRaised("o2"));
		interpreter.exit();
	}
}
