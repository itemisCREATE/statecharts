/** Generated by YAKINDU Statechart Tools code generator. */
package org.yakindu.sct.simulation.core.sexec.test;
import org.eclipse.xtext.junit4.InjectWith;
import org.eclipse.xtext.junit4.XtextRunner;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.yakindu.sct.model.sexec.ExecutionFlow;
import org.yakindu.sct.model.sexec.interpreter.test.util.AbstractExecutionFlowTest;
import org.yakindu.sct.model.sexec.interpreter.test.util.SExecInjectionProvider;
import org.yakindu.sct.test.models.SCTUnitTestModels;
import com.google.inject.Inject;
import static org.junit.Assert.*;

/**
 * Unit TestCase for StatechartEntryAndExitActions
 */
@SuppressWarnings("all")
@RunWith(XtextRunner.class)
@InjectWith(SExecInjectionProvider.class)
public class StatechartEntryExitActions extends AbstractExecutionFlowTest {
	
	@Before
	public void setup() throws Exception{
		ExecutionFlow flow = models.loadExecutionFlowFromResource("statechartactions/StatechartEntryAndExitActions.sct");
		initInterpreter(flow);
	}
	@Test
	public void entryActionsAreExecutedOnEnteringStatechart() throws Exception {
		assertTrue(getInteger("x") == 0l);
		assertTrue(getInteger("y") == 0l);
		setBoolean("b", true);
		interpreter.enter();
		assertTrue(getInteger("x") == 5l);
		assertTrue(getInteger("y") == 3l);
	}
	@Test
	public void entryActionGuardsAreEvaluatedOnEnteringStatechart() throws Exception {
		assertTrue(getInteger("x") == 0l);
		assertTrue(getInteger("y") == 0l);
		setBoolean("b", false);
		interpreter.enter();
		assertTrue(getInteger("x") == 2l);
		assertTrue(getInteger("y") == 3l);
	}
	@Test
	public void exitActionsAreExecutedOnExitingStatechart() throws Exception {
		interpreter.enter();
		setBoolean("b", true);
		interpreter.exit();
		assertTrue(getInteger("x") == 6l);
		assertTrue(getInteger("y") == 2l);
	}
	@Test
	public void exitActionGuardsAreEvaluatedOnExitingStatechart() throws Exception {
		interpreter.enter();
		setBoolean("b", false);
		interpreter.exit();
		assertTrue(getInteger("x") == 8l);
		assertTrue(getInteger("y") == 2l);
	}
}
