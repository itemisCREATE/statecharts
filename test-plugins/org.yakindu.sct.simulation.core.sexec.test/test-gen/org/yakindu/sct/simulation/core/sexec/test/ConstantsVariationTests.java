/** Generated by YAKINDU Statechart Tools code generator. */
package org.yakindu.sct.simulation.core.sexec.test;
import org.eclipse.xtext.junit4.InjectWith;
import org.eclipse.xtext.junit4.XtextRunner;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.yakindu.sct.model.sexec.ExecutionFlow;
import org.yakindu.sct.model.sexec.interpreter.test.util.AbstractExecutionFlowTest;
import org.yakindu.sct.model.sexec.interpreter.test.util.SExecInjectionProvider;
import org.yakindu.sct.test.models.SCTUnitTestModels;
import com.google.inject.Inject;
import static org.junit.Assert.*;

/**
 * Unit TestCase for ConstantsVariation
 */
@SuppressWarnings("all")
@RunWith(XtextRunner.class)
@InjectWith(SExecInjectionProvider.class)
public class ConstantsVariationTests extends AbstractExecutionFlowTest {
	
	@Before
	public void setup() throws Exception{
		ExecutionFlow flow = models.loadExecutionFlowFromResource("ConstantsVariation.sct");
		initInterpreter(flow);
	}
	@Test
	public void constantDefinition() throws Exception {
		interpreter.enter();
		assertTrue(isStateActive("StateA"));
		assertTrue(getBoolean("b2") == true);
		assertTrue(getBoolean("named.b") == true);
		assertTrue(getInteger("i2") == 5l);
		assertTrue(getInteger("named.i") == 5l);
		assertTrue(getReal("r2") == 5l);
		assertTrue(getReal("named.r") == 5l);
		assertTrue(getString("str2").equals("String"));
		assertTrue(getString("named.str").equals("String"));
		timer.timeLeap(getCyclePeriod());
		assertTrue(interpreter.isFinal());
	}
}
