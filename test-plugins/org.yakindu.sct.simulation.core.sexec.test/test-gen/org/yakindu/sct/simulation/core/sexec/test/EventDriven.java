/** Generated by YAKINDU Statechart Tools code generator. */
package org.yakindu.sct.simulation.core.sexec.test;
import org.eclipse.xtext.junit4.InjectWith;
import org.eclipse.xtext.junit4.XtextRunner;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.yakindu.sct.model.sexec.ExecutionFlow;
import org.yakindu.sct.model.sexec.interpreter.test.util.AbstractExecutionFlowTest;
import org.yakindu.sct.model.sexec.interpreter.test.util.SExecInjectionProvider;
import org.yakindu.sct.test.models.SCTUnitTestModels;
import com.google.inject.Inject;
import static org.junit.Assert.*;

/**
 * Unit TestCase for eventDriven
 */
@SuppressWarnings("all")
@RunWith(XtextRunner.class)
@InjectWith(SExecInjectionProvider.class)
public class EventDriven extends AbstractExecutionFlowTest {
	
	@Before
	public void setup() throws Exception{
		ExecutionFlow flow = models.loadExecutionFlowFromResource("EventDriven.sct");
		initInterpreter(flow, true);
	}
	@Test
	public void test() throws Exception {
		interpreter.enter();
		assertTrue(isRaised("running"));
		raiseEvent("inEvent");
		assertTrue(isRaised("oe1"));
		assertTrue(isRaised("oe2"));
		assertTrue(isRaised("oe3"));
		assertTrue(isStateActive("StateD"));
		raiseEvent("inEventBool", true);
		assertTrue(isStateActive("StateE"));
		assertTrue(isStateActive("State2"));
		raiseEvent("inEvent");
		assertTrue(isStateActive("StateF"));
		raiseEvent("namedI.namedInEvent");
		assertTrue(interpreter.isFinal());
		interpreter.exit();
	}
}
