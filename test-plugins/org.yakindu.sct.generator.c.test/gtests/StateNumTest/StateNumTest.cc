/* Generated by YAKINDU Statechart Tools code generator. */

#include "gtest/gtest.h"
#include "HistoryWithoutInitialStep.h"

#include "sc_timer_service.h"



//! The timers are managed by a timer service. */
static sc_unit_timer_service_t timer_service;

static HistoryWithoutInitialStep statechart;

class StateNumTest : public ::testing::Test{
	protected:
	virtual void SetUp() {
		historyWithoutInitialStep_init(&statechart);
		sc_timer_service_init(
			&timer_service,
			0,
			(sc_run_cycle_fp) &historyWithoutInitialStep_runCycle,
			false,
			200,
			&statechart
		);
	}
};


TEST_F(StateNumTest, stateNumTest) {
#ifdef HISTORYWITHOUTINITIALSTEP_STATE_COUNT
EXPECT_TRUE(HISTORYWITHOUTINITIALSTEP_STATE_COUNT == 6);
#else
EXPECT_TRUE(false);
#endif
}

