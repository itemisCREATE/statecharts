/** Generated by YAKINDU Statechart Tools code generator. */
#include <string>
#include "gtest/gtest.h"
#include "SuperStepOnEnter.h"
#include "sc_runner.h"
#include "sc_types.h"

namespace  {

supersteps::SuperStepOnEnter* statechart;



//! The timers are managed by a timer service. */
static SctUnitRunner * runner;

class SuperStepOnEnterTest : public ::testing::Test{
	protected:
	virtual void SetUp() {
		statechart = new supersteps::SuperStepOnEnter();
		statechart->init();
		runner = new SctUnitRunner(
			statechart,
			true,
			200
		);
	}
	virtual void TearDown() {
		delete statechart;
		delete runner;
	}
};


TEST_F(SuperStepOnEnterTest, test) {
	
	statechart->enter();
	
	EXPECT_TRUE(statechart->isStateActive(supersteps::SuperStepOnEnter::r1_StateB));
	
	EXPECT_TRUE(statechart->isStateActive(supersteps::SuperStepOnEnter::r2_StateB));
	
	statechart->exit();
	
	
}

}

