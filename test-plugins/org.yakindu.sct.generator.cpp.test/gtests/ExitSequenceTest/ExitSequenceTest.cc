/* Generated by YAKINDU Statechart Tools code generator. */
#include <string>
#include "gtest/gtest.h"
#include "ExitSequence.h"
#include "sc_runner.h"
#include "sc_types.h"

namespace  {

ExitSequence* statechart;



//! The timers are managed by a timer service. */
static SctUnitRunner * runner;

class ExitSequenceTest : public ::testing::Test{
	protected:
	virtual void SetUp() {
		statechart = new ExitSequence();
		statechart->init();
		runner = new SctUnitRunner(
			statechart,
			true,
			200
		);
	}
	virtual void TearDown() {
		delete statechart;
		delete runner;
	}
};


TEST_F(ExitSequenceTest, testFrom1A) {
	
	statechart->enter();
	
	EXPECT_TRUE((statechart->getDefaultSCI()->get_canary()== 0l));
	
	statechart->getDefaultSCI()->raise_e();
	
	EXPECT_TRUE((statechart->getDefaultSCI()->get_canary()== 0l));
	
	EXPECT_TRUE(statechart->isStateActive(ExitSequence::main_region_StateA_inner1_State1A));
	
	statechart->exit();
	
	EXPECT_TRUE((statechart->getDefaultSCI()->get_canary()== 1l));
	
	
}
TEST_F(ExitSequenceTest, testFrom1B) {
	
	statechart->enter();
	
	EXPECT_TRUE((statechart->getDefaultSCI()->get_canary()== 0l));
	
	statechart->getDefaultSCI()->set_guard(true);
	
	statechart->getDefaultSCI()->raise_e();
	
	EXPECT_TRUE((statechart->getDefaultSCI()->get_canary()== 0l));
	
	EXPECT_TRUE(statechart->isStateActive(ExitSequence::main_region_StateA_inner1_State1B));
	
	statechart->exit();
	
	EXPECT_TRUE((statechart->getDefaultSCI()->get_canary()== 1l));
	
	
}


}
