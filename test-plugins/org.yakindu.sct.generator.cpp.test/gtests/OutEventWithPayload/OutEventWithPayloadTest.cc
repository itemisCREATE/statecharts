/** Generated by YAKINDU Statechart Tools code generator. */
#include <string>
#include "gtest/gtest.h"
#include "OutEventWithPayload.h"
#include "sc_runner.h"
#include "sc_types.h"

namespace  {

events::OutEventWithPayload* statechart;



//! The timers are managed by a timer service. */
static SctUnitRunner * runner;

class OutEventWithPayloadTest : public ::testing::Test{
	protected:
	virtual void SetUp() {
		statechart = new events::OutEventWithPayload();
		statechart->init();
		runner = new SctUnitRunner(
			statechart,
			true,
			200
		);
	}
	virtual void TearDown() {
		delete statechart;
		delete runner;
	}
};


TEST_F(OutEventWithPayloadTest, out_events_must_have_payload) {
	
	statechart->enter();
	
	statechart->raise_i(21);
	
	EXPECT_TRUE(statechart->isRaised_o());
	
	EXPECT_TRUE(statechart->get_o_value()== 42);
	
	statechart->exit();
	
	
}

}

