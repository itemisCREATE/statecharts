package tests
testclass StaticChoice for statechart StaticChoice{
	
	operation StaticChoiceTest(){ 
		enter
		assert active(StaticChoice.main_region.Start)
		cycle
		//assert active(StaticChoice.main_region.B) // doesn't work atm		 
	} 
}
	